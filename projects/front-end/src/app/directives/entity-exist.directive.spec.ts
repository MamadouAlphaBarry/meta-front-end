import { EntityExistDirective } from './entity-exist.directive';

describe('EntityExistDirective', () => {
  it('should create an instance', () => {
    const directive = new EntityExistDirective();
    expect(directive).toBeTruthy();
  });
});
